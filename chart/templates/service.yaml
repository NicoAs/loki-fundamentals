apiVersion: v1
kind: Service
metadata:
  name: {{ include "loki-fundamentals.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "loki-fundamentals.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "loki-fundamentals.selectorLabels" . | nindent 4 }}
