apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "loki-fundamentals.fullname" . }}-test-connection"
  labels:
    {{- include "loki-fundamentals.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "loki-fundamentals.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
